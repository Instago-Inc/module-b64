## v1.0.7 - Docs Polish
Concise documentation and site copy for the b64 base64 utility now better highlights the compact helpers and call-outs for encodeJson failure behavior to aid discoverability.

### Changed
- Reworded the README and docs landing copy to emphasize the short-string helpers, URL-safe encoding, and low-friction use cases.
- Clarified the `encodeJson` reference so it notes that it returns an empty string when it cannot encode the input.

## v1.0.5 - Release Metadata Tune-Up
Patch release for the b64 base64 utility module with versioning housekeeping and release metadata keywords for discoverability.

### Changed
- Bumped the package version for a patch release.

## v1.0.4 - Documentation Page Update
Patch release for the b64 base64 utility module focusing on documentation page content and presentation, with clearer module keywords for discoverability.

### Changed
- Refreshed the docs landing page content and styling.

## v1.0.3 - Release Metadata Refresh
Patch release for the b64 base64 utility module with versioning metadata updates and release hygiene keywords.

### Changed
- Bumped package version for a patch release.

## v1.0.2 - Maintenance Patch Release
Patch release for the b64 base64 utility module focusing on release metadata and versioning hygiene.

### Changed
- Bumped package version for a patch release.

## v1.0.1 - Patch Release Metadata
Patch release for the b64 base64 utility module with release metadata updates; no functional changes.

### Changed
- Updated package version for release.
